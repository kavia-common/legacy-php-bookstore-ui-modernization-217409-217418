Run the following to install Vite-based toolchain:

npm install
# If previously installed with CRA-only deps, ensure these dev deps are present:
npm i -D vite @vitejs/plugin-react typescript @types/react @types/react-dom
