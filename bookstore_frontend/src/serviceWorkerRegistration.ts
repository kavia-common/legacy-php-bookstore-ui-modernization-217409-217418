 // CRA-style serviceWorkerRegistration is intentionally disabled.

 // PUBLIC_INTERFACE
 export function register(): void {
   /** No-op: Service workers are not used by this app. */
 }

 // PUBLIC_INTERFACE
 export function unregister(): void {
   /** No-op: Service workers are not used by this app. */
 }
