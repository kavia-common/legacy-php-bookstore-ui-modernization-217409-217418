 // This project intentionally does not use a service worker.
 // This stub prevents accidental imports from older templates.

 // PUBLIC_INTERFACE
 export default function registerServiceWorker(): void {
   /** No-op stub. Service workers are disabled. */
 }
