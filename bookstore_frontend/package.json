{
  "name": "modern-bookstore-ui",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "bootstrap": "^5.3.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.26.1",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "react-app-rewired": "^2.2.1",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "start": "cross-env PORT=3000 react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --watchAll=false",
    "eject": "react-scripts eject",
    "typecheck": "tsc --noEmit"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "notes": {
    "serviceWorker": "No service worker is used in this project. Ensure any previous service worker registrations are removed or unregistered in the browser devtools > Application > Service Workers."
  }
}
